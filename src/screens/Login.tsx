import { LoginFormCard } from "@/components/LoginFormCard/login-form-card";

const Login = () => {
  return (
    <div className="bg-neutral-100">
      <LoginFormCard />
    </div>
  );
};

export default Login;
